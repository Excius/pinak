meta {
  name: Admin - List Tax Classes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/tax-classes/admin
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  List tax classes with full admin payloads. Requires Moderator or Admin.
}
