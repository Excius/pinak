meta {
  name: List Tax Classes
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/tax-classes
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  List available tax classes (public, requires authentication).
}
