meta {
  name: Admin Get Coupon by Code
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/coupons/admin/:code
  body: none
  auth: inherit
}

params:path {
  code: FLAT200
}

vars:pre-request {
  baseUrl: http://localhost:3000
}
