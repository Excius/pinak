meta {
  name: Admin Validate Coupon
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/coupons/admin/validate
  body: json
  auth: inherit
}

body:json {
  {
    "code": "FLAT200",
    "cartTotal": 2500
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}
