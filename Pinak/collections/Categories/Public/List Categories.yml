info:
  name: List Categories
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/categories"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: parentId
      value: cmnj36d64001d2amzgp45f801
      type: query
      disabled: true
    - name: withChildren
      value: "false"
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: List categories (public).
