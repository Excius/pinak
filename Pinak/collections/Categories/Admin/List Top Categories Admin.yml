info:
  name: List Top Categories (Admin)
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/categories/admin/top"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: List top-level categories (admin — full content with images).
