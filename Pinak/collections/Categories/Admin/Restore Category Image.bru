meta {
  name: Restore Category Image
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/api/v1/categories/admin/images/{{id}}/restore
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  id: your-image-id-here
  authToken: your-jwt-token-here
}

docs {
  Restore a soft-deleted category image (admin only).
}
