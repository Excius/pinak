meta {
  name: List Categories Admin
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/categories/admin?parentId=
  body: none
  auth: inherit
}

params:query {
  parentId: 
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  List categories (admin — full content with images).
}
