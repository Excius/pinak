meta {
  name: List Filter Groups
  type: http
  seq: 40
}

get {
  url: {{baseUrl}}/api/v1/filters/groups
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  List filter groups (authenticated).
}