info:
  name: Restore Image
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/products/admin/images/{{id}}/restore"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-moderator-or-admin-jwt-token-here
    - name: id
      value: cmnjdin8y0002ccmzubzz03sn

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Route uses ProductTypes.RestoreProduct schema
  Params: id
  Query: none
  Body: {}
