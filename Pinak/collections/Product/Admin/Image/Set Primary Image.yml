info:
  name: Set Primary Image
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/products/admin/images/{{imageId}}/primary"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-moderator-or-admin-jwt-token-here
    - name: imageId
      value: cmpqrgn59000045mzw0ja03eq

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Schema: ProductTypes.SetPrimaryImage
  Params: imageId
  Query: none
  Body: {}
