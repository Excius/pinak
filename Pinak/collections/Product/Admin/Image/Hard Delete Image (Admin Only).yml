info:
  name: Hard Delete Image (Admin Only)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/products/admin/images/{{id}}/hard"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-admin-jwt-token-here
    - name: id
      value: cmnjf3f5w0000zimzoid3u625

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Route uses ProductTypes.HardDeleteProduct schema
  Params: id
  Query: none
  Body: {}
