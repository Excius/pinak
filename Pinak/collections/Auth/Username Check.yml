info:
  name: Username Check
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/username?username=kjdhdf5"
  params:
    - name: username
      value: kjdhdf5
      type: query
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
