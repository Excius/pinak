meta {
  name: Verify password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/auth/verify-password
  body: json
  auth: inherit
}

body:json {
  {
    "token": "",
    "newPassword":"12345678"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}
