meta {
  name: Forgot password
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/auth/forgot-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "daksh.k23@iiits.in",
    "platform":"web"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

settings {
  encodeUrl: true
  timeout: 0
}
