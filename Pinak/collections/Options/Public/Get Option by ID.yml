info:
  name: Get Option by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/options/{{optionId}}"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: optionId
      value: cmnj36axa000h2amzao5w9d1p

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Get an option by ID (public).
