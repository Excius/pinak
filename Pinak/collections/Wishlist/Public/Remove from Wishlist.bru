meta {
  name: Remove from Wishlist
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/v1/wishlist/items/{{itemId}}
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  itemId: cmo01b1rt00006amz0r417bpy
  authToken: your-jwt-token-here
}

docs {
  Remove an item from the authenticated user's own wishlist.
}
