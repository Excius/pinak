meta {
  name: Add to Wishlist
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/wishlist/items
  body: json
  auth: inherit
}

body:json {
  {
    "productVariantId": "your-product-variant-id-here"
  }
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  Add a product variant to authenticated user's wishlist.
}
