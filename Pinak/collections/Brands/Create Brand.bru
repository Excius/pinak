meta {
  name: Create Brand
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/brands
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Nivea",
    "slug": "nivea",
    "logoUrl": "https://example.com/logo.png",
    "isActive": true
  }
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  Create a new brand (Moderator/Admin required).
}