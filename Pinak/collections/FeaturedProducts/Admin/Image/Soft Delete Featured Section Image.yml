info:
  name: Soft Delete Featured Section Image
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/featured-sections/admin/images/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: id
      value: cmtjwhotu004ls3mzbu58uamt
    - name: authToken
      value: your-jwt-token-here

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Soft-delete a featured section image (admin only).
