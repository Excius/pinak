info:
  name: Create Weight Class
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/v1/weight-classes"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Gram",
        "unit": "g"
      }
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Create a weight class (Moderator/Admin required).
