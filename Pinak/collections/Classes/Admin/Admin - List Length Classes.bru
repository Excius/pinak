meta {
  name: Admin - List Length Classes
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/admin/length-classes
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  List length classes with full admin payloads. Requires Moderator or Admin.
}
