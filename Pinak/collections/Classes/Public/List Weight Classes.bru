meta {
  name: List Weight Classes
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/weight-classes
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: your-jwt-token-here
}

docs {
  List weight classes (public, requires authentication).
}
