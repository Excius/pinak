meta {
  name: List Length Classes
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/length-classes
  body: none
  auth: inherit
}

headers {
  Content-Type: application/json
}

vars:pre-request {
  baseUrl: http://localhost:3000
}

docs {
  List length classes (public).
}