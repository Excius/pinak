info:
  name: Increment Combo Kit View
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/combo-kits/{{id}}/increment-view"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here
    - name: id
      value: cmnj36wx700532amznio2194z

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Increment combo kit view counter (authenticated route).
