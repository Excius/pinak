info:
  name: Get Combo Kit by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/v1/combo-kits/{{id}}"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: id
      value: cmnj36wx700532amznio2194z

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Get combo kit details by ID.
