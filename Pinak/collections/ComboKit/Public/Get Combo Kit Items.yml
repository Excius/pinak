info:
  name: Get Combo Kit Items
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/combo-kits/{{id}}/items"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: id
      value: cmnj36wx700532amznio2194z

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: Get item list for a combo kit by combo ID.
