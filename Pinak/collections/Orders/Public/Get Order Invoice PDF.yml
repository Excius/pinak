info:
  name: Get Order Invoice PDF
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/v1/orders/{{orderId}}/invoice/pdf"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here
    - name: orderId
      value: cmu7e95px000501pat3xawo99

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: "Schema: OrderTypes.GetInvoicePdf"
