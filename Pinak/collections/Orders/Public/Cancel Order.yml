info:
  name: Cancel Order
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/v1/orders/{{orderId}}/cancel"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here
    - name: orderId
      value: cmtfpqssb0001zwmzggeuic09

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: "Schema: OrderTypes.CancelOrder"
