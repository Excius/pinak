info:
  name: Get Order Invoice HTML
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/orders/{{orderId}}/invoice"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here
    - name: orderId
      value: cmui4p0vw0000ncmzetjsj7hl

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: "Schema: OrderTypes.GetInvoiceHtml"
