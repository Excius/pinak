info:
  name: Get Order by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/orders/{{orderId}}"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000
    - name: authToken
      value: your-jwt-token-here
    - name: orderId
      value: cmoadvikb00014wmzgkrjxalv

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: "Schema: OrderTypes.GetOrderById"
