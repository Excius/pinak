info:
  name: Admin - Hard Delete Order
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/orders/admin/:id/hard"
  params:
    - name: id
      value: cmnj36zfb005k2amzvhzkvae8
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Permanently deletes an order from the database.
  Only accessible by ADMIN users.
