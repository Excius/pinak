info:
  name: Soft Delete Dynamic Asset
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/dynamic-assets/admin/{{assetId}}"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

runtime:
  variables:
    - name: assetId
      value: cmtq25aby0000d8mz95tardsl

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Schema: DynamicAssetAdminTypes.SoftDeleteDynamicAsset
  Route: DELETE /dynamic-assets/admin/:id
  Access: moderator/admin
