info:
  name: Admin List Dynamic Assets
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/dynamic-assets/admin/all?includeDeleted=false"
  headers:
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Schema: DynamicAssetAdminTypes.AdminListDynamicAssets
  Route: GET /dynamic-assets/admin/all
  Access: moderator/admin
  Query: includeDeleted (boolean, optional)
