info:
  name: List Dynamic Assets (Public)
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/dynamic-assets"
  headers:
    - name: Content-Type
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |-
  Schema: DynamicAssetTypes.ListDynamicAssets
  Route: GET /dynamic-assets
  Access: public
