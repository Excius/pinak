info:
  name: Delete Address
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/addresses/:id"
  params:
    - name: id
      value: cmobc381d0001wqmzzvujiqym
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
