info:
  name: Get Address by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/addresses/:id"
  params:
    - name: id
      value: cmobc2wbu0000wqmzysukkgu5
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
