services:
  api:
    build:
      context: .
      dockerfile: ./apps/api/Dockerfile
    container_name: pinak-api
    restart: unless-stopped
    env_file:
      - ./apps/api/.env
    ports:
      - "3000:3000"